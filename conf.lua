function love.conf(t)

    t.window.title = "plamt"
    t.window.width = 1280   -- original 256
    t.window.height = 720   -- original 144
    t.console = true        -- does not work on mac apparently

end